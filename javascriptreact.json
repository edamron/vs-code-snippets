{
	// Place your snippets for javascriptreact here. Each snippet is defined under a snippet name and has a prefix, body and 
	// description. The prefix is what is used to trigger the snippet and the body will be expanded and inserted. Possible variables are:
	// $1, $2 for tab stops, $0 for the final cursor position, and ${1:label}, ${2:another} for placeholders. Placeholders with the 
	// same ids are connected.
	// Example:
	// "Print to console": {
	// 	"prefix": "log",
	// 	"body": [
	// 		"console.log('$1');",
	// 		"$2"
	// 	],
	// 	"description": "Log output to console"
	// }
	"Functional Component": {
		"prefix": "rfc",
		"body": [
			"import React, { useState, useEffect } from 'react';",
			"import PropTypes from 'prop-types';",
			"// React Native ONLY -> import { StyleSheet, Text, View } from 'react-native';",
			"",
			"const ${1:CompName} = ({ ${2:prop1Name}, ${3:prop2Name} }) => {",
			"	$0",
			"};",
			"",
			"$1.propTypes = {",
			"	$2: PropTypes.string.isRequired, // <- e.g., a required string",
			"	$3: PropTypes.func, // <- e.g., an optional function",
			"};",
			"",
			"$1.displayName = '$1'",
			"",
			"export default $1;",
		],
		"description": "A complete React functional component",
	},
	"console.log()": {
		"prefix": "clog",
		"description": "console.log(...)",
		"body": "console.log('${1:what?}');$0",
	},
}